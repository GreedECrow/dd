const mongoose = require('mongoose')

const { Schema } = mongoose

const Dd = new Schema({
    name: String,
    img_url: String,
    director: String,
    year: Number
})

const DdSheet = mongoose.model('DD', DDSchema)


module.exports = Dd